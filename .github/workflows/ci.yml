name: CI Demo

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Run a Python script
        run: python -c "print('CI/CD is working!')"
